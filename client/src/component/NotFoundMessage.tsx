import React from 'react';

const NotFoundMessage = () => {
    return (
        <div>
            <div className="fixed top-1/3 left-1/2 -translate-x-1/2 text-center text-sky-50 text-4xl">Tasks not found!</div>
        </div>
    );
};

export default NotFoundMessage;